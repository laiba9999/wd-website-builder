node_modules
.next
out
.env
venv
.env.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.history/
.env.local
.claude/settings.json
# .github is intentionally NOT ignored — .github/workflows/keepalive.yml has to
# reach GitHub for the every-3-days ping that stops Supabase pausing the project.
